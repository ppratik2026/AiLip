# AiLip — Python dependencies
# For full install (GPU + models) run: bash setup.sh
# For manual install: pip install -r requirements.txt
#
# Note: PyTorch must be installed separately to pick the right CUDA version:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# Core pipeline
numpy>=1.24
scipy>=1.10
opencv-python>=4.8
pillow>=10.0
imageio>=2.31
imageio-ffmpeg>=0.4.9
tqdm>=4.65
einops>=0.7
omegaconf>=2.3
pyyaml>=6.0

# HuggingFace
huggingface_hub>=0.20
accelerate>=0.26
diffusers>=0.25
transformers>=4.37

# Enhancement & Upscaling
basicsr>=1.4.2
facexlib>=0.3.0
gfpgan>=1.3.8
realesrgan>=0.3.0

# Audio
openai-whisper>=20231117

# Webapp
flask>=3.0
gunicorn>=21.2
gtts>=2.5
pyttsx3>=2.90
